# mesh-router-gateway Development Environment
# Copy this file to .env and fill in your values

# =============================================================================
# DOMAIN CONFIGURATION (Required)
# =============================================================================

# Domain suffix for routing (e.g., nsl.sh, example.com)
# This is the base domain that all user subdomains are under
SERVER_DOMAIN=nsl.sh

# =============================================================================
# BACKEND CONFIGURATION (Required)
# =============================================================================

# Resolution backend API URL
# - For local development with backend running: http://mesh-router-backend:8192
# - For staging against remote backend: https://your-staging-server.com/router/api
BACKEND_URL=http://mesh-router-backend:8192

# =============================================================================
# CACHE CONFIGURATION (Optional)
# =============================================================================

# Resolution cache TTL in seconds
# Lower values = more backend calls but fresher data
# Higher values = fewer backend calls but stale data risk
CACHE_TTL=60

# =============================================================================
# BUILD CONFIGURATION (Optional)
# =============================================================================

# Build version tag (for image labeling)
BUILD_VERSION=dev
